/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  // If deploying to GitHub Pages project site (username.github.io/repo-name),
  // uncomment and set basePath to your repo name:
  // basePath: "/Omni-Link-website",
  // assetPrefix: "/Omni-Link-website/",
};

module.exports = nextConfig;
